Despues de mazclar cambios en repositorio remoto, es neceario mezclr cambios en el repo local.
Para esto es necesario hacer un git fetch y luego un git merge con mi repo local
Para que sea mucho mas eficciente use git pull
